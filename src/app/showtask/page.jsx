import React from 'react'
import Link from 'next/link';

export const metadata = {
    title: "Todo : Show Task",
    description: "Show task that user have to perform",
};


const showtask = () => {
    return (
        <h1>showtask</h1>
    )

}

export default showtask